import { describe,  test } from "vitest";

describe("Delete customer", () => {
	test("It should delete a costumer", async () => {});

	test.todo("It should throw an error if the costumer was not found");
});
